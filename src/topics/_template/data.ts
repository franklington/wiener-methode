// REPLACE: the real, cited figures this topic's units read from.
// Every field here should be traceable to a `sourceId` in ./sources.ts.
export const exampleFigure = {
  value: 42,
  sourceId: "replace-me",
};
